// this test doesn't work as we need to ts compile
// all the /mock/ files to get the correct file paths and
// require them properly...

// import { describe, it } from 'vitest'
// import express from 'express'
// import path from 'path'
// import { createRoutes } from '../src'

// const app = express()

// const routesDirectory = path.join(__dirname, '/mock')

// describe('createRoutes', () => {
//   it('should create the routes and register to express', async () => {
//     await createRoutes(app, { routesDirectory })
//     console.log(app._router)
//   })
// })
